import 'next';

// Intentionally left blank.
// Previously augmented `next` with a `PageProps` interface which
// conflicts with Next.js App Router's generated route types.
// Define route prop types locally per page instead.
